type Theme = {
  colors: {
    primary: string;
    secondary: string;
    background: string;
    onPrimary: string;
    onSecondary: string;
    onBackground: string;
  };
  fonts: {
    primary: string;
  };
  fontSizes: {
    small: string;
    medium: string;
    large: string;
  };
};

export default Theme;
