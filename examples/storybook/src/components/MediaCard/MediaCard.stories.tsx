import { Story } from '@storybook/react';
import Button from '../Button/Button';
import Card, { CardProps } from './MediaCard';

export default {
  title: 'Display/MediaCard',
  component: Card,
};

// export const MediaCard: Story<CardProps> = (args) => (
//   <Card {...args}>
//     <p>Movie Title</p>
//     <Button>Add to List</Button>
//   </Card>
// );
